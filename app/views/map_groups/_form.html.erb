
<%= simple_form_for(@map_group) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :Name %>
  </div>

  <br/>

  <%# <% if @map_group.client_group.nil? && @types.length > 0 %>
  
    <%# <div class="field"> %>
      <%# <%= f.input :client_group_id, collection: @types, value_method: :first, label_method: :second, selected: 0 %>
    <%# </div> %>

    <%# <br/> %>

  <%# <% end %>

  <div class="form-actions">
    <%= f.button :submit, class: "waves-effect waves-light blue lighten-1 btn-small white-text" %>
  </div>

  <br/>
<% end %>
